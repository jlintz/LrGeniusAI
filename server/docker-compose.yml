# geniusai-server mit Docker Compose
# Start: docker compose up -d
# Stop:  docker compose down
# Logs:  docker compose logs -f geniusai-server

services:
  geniusai-server:
    build: .
    image: geniusai-server:latest
    container_name: geniusai-server
    restart: unless-stopped
    ports:
      - "19819:19819"
    volumes:
      # ChromaDB und Logs persistent im Ordner ./data
      - ./data:/data
      # OpenCLIP/Hugging Face + InsightFace: Modell-Downloads persistent (große Dateien)
      - ./models:/models
      # Persist Google Cloud CLI config + ADC login across container restarts/rebuilds
      - ./gcloud:/root/.config/gcloud
    environment:
      - GENIUSAI_HOST=0.0.0.0
      - GENIUSAI_PORT=19819
      - CLOUDSDK_CONFIG=/root/.config/gcloud
